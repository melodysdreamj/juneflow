name: juneflow
description: "JuneFlow Project"
#publish_to: 'none'
#repository: https://github.com/melodysdreamj/juneflow

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  after_layout: #@add
  device_preview:
  flutter:
    sdk: flutter
  flutter_screenutil:
  flutter_svg: #@add
  gap: #@add
  june_flow_util:
  styled_widget: #@add
  modal_bottom_sheet: #@add
  keyboard_attachable: #@add
  flutter_zoom_drawer: #@add
  bot_toast:

dev_dependencies:
  bot_toast_module: #@add
  device_preview_module: #@add
  flutter_lints:
  flutter_lints_module: #@add
  flutter_test:
    sdk: flutter
  screenutil_module: #@add

flutter:
  uses-material-design: true